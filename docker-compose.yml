services:
  galo-routine:
    build: .
    ports:
      - "${PORT:-3200}:3200"
    volumes:
      - galo-data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/galo-routine.db
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  galo-data:
